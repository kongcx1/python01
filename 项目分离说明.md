# 项目分离说明

已将 `python01` 文件夹下的三个项目独立成可以单独运行的项目文件夹。

## 独立项目列表

### 1. portal_site_project - 门户网站项目

**位置：** `python01/portal_site_project/`

**功能：** 独立的门户网站，包含内容管理系统（CMS）和多语言支持

**启动方式：**
```bash
cd portal_site_project
pip install -r requirements.txt
uvicorn cms_server:app --host 0.0.0.0 --port 8000
```

或使用启动脚本：
- Windows: `start.bat`
- Linux/Mac: `./start.sh`

**访问地址：**
- 前端首页: http://localhost:8000/
- 管理后台: http://localhost:8000/admin

**主要文件：**
- `cms_server.py` - FastAPI 服务器
- `index.html`, `admin.html` - 前端页面
- `content.json` - 内容数据
- `requirements.txt` - Python 依赖

---

### 2. web_app_project - Web 应用项目

**位置：** `python01/web_app_project/`

**功能：** Telegram 视频下载器的 Web 管理界面

**启动方式：**
```bash
cd web_app_project
pip install -r requirements.txt
uvicorn server:app --host 0.0.0.0 --port 8080
```

或使用启动脚本：
- Windows: `start.bat`
- Linux/Mac: `./start.sh`

**访问地址：**
- Web 界面: http://localhost:8080/

**主要文件：**
- `server.py` - FastAPI 服务器
- `index.html` - 主页面
- `app.js` - 前端逻辑
- `styles.css` - 样式文件
- `requirements.txt` - Python 依赖

**注意：** 此 Web 应用需要连接到后端 API 服务器才能完整工作。请确保后端服务已启动并配置正确的 API 地址。

---

### 3. server_tools_project - 服务器工具项目

**位置：** `python01/server_tools_project/`

**功能：** 服务器部署和管理工具集合

**主要工具：**
- `setup_server.sh` - 服务器环境初始化
- `install_service.sh` - 安装 systemd 服务
- `manage.sh` - 服务管理（启动/停止/重启/查看状态）
- `one_click_deploy.sh` - 一键部署脚本
- `one_click_submit.sh` - 一键提交任务脚本
- `telegram_downloader.service` - systemd 服务配置

**使用方法：** 详见 `server_tools_project/README.md`

**注意：** 这些工具需要与主服务器应用（server_app.py）配合使用。

---

## 项目结构对比

### 原始结构（杂糅在一起）
```
python01/
├── portal_site/          # 门户网站文件
├── web_app/              # Web 应用文件
├── server_tools/         # 服务器工具
├── server_app.py         # 主服务器应用
└── ...其他文件
```

### 新结构（独立项目）
```
python01/
├── portal_site_project/  # ✅ 独立的门户网站项目
│   ├── cms_server.py
│   ├── index.html
│   ├── requirements.txt
│   ├── README.md
│   └── start.bat / start.sh
│
├── web_app_project/      # ✅ 独立的 Web 应用项目
│   ├── server.py
│   ├── index.html
│   ├── requirements.txt
│   ├── README.md
│   └── start.bat / start.sh
│
├── server_tools_project/ # ✅ 独立的服务器工具项目
│   ├── setup_server.sh
│   ├── manage.sh
│   ├── README.md
│   └── ...其他工具脚本
│
└── ...原始文件（保持不变）
```

---

## 使用建议

1. **portal_site_project** - 可以完全独立运行，用于展示门户网站和管理内容
2. **web_app_project** - 可以独立运行提供 Web 界面，但需要配置后端 API 地址
3. **server_tools_project** - 作为工具集使用，配合主服务器应用部署

每个项目都包含：
- ✅ 独立的 `requirements.txt`
- ✅ 详细的 `README.md` 说明文档
- ✅ 启动脚本（Windows 和 Linux/Mac）
- ✅ 完整的项目文件

现在这三个项目可以分别独立运行和部署了！
